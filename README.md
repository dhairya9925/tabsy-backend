# Splitwise Buddy Backend API

FastAPI backend providing REST API endpoints for Splitwise Buddy.
All endpoints require Supabase JWT authentication and follow the standard ResponseEnvelope `{ data, error, meta }`.
